<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.gocle.include">

    <sql id="paging-start">
        SELECT *
        FROM (
            SELECT 
                data.*,
                ROW_NUMBER() OVER (ORDER BY REG_DT DESC) AS row_num,
                COUNT(*) OVER() AS total_count
            FROM (
    </sql>

    <sql id="paging-end">
            ) AS data
        ) AS data
        <![CDATA[
        WHERE row_num > ( (#{pageIndex, jdbcType=INTEGER} - 1) * #{pageSize, jdbcType=INTEGER} )
          AND row_num <= (#{pageIndex, jdbcType=INTEGER} * #{pageSize, jdbcType=INTEGER})
        ]]>
    </sql>

</mapper>